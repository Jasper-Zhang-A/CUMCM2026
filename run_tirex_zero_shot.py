"""Placeholder: TiRex zero-shot runner source is generated in local workspace.

The full implementation keeps a strict causal forecasting contract:
context interval_end <= decision_time; target begins at decision_time.
"""

from pathlib import Path

if __name__ == "__main__":
    raise SystemExit(
        "Copy the generated TiRex baseline runner from the experiment workspace before execution."
    )
