# CUMCM 2026 C题当前成果交接说明

交接日期：2026-09-11  
当前范围：附件2的 Load / PV 时间序列预测，已完成 Seasonal B0–B4 与 DLinear Stage 1。

## 先看这三个文件

1. `TIME_PROTOCOL.md`：冻结的10分钟区间语义与 EventTime 因果协议。
2. `BASELINE_REPORT.md`：B0–B4 的完整结果与结论。
3. `MODEL_COMPARISON.md`：DLinear 选型、回测、低数据实验和最终比较。

`MODEL_SELECTION.md` 单独记录1月模型选择，`DATA_AUDIT.md` 记录原始数据审计。

## 已验证的主要结论

| 序列 | 当前最强简单基线 | 基线 MAE | DLinear MAE | DLinear 相对变化 |
|---|---|---:|---:|---:|
| Load | B2 Last Week Same Slot | 176.7957 kW | 182.9798 kW | -3.50% |
| PV | B3 Previous 7-day Same-Slot Mean | 153.3997 kW | 201.6898 kW | -31.48% |

DLinear 的 Load 选用 `seq_len=1008`、49轮，PV选用 `seq_len=432`、46轮；只使用1月25–31日验证结果选择，2–12月不再调结构。DLinear 未按预注册的 MAE 门槛超过强基线，因此未追加2027/2028种子，也没有继续 PatchTST。

全部98项因果性测试通过：原59项、learned-model预检31项、产物后检8项。未来污染测试、scaler截止时间、训练窗口截止时间和checkpoint数据哈希均已覆盖。

## 目录

- `inputs/C题/`：题目PDF与本阶段使用的附件2。
- `artifacts/baselines/`：canonical actual、B0–B4逐点预测、lineage和运行状态。
- `artifacts/metrics/`、`artifacts/figures/`：Seasonal指标和图。
- `artifacts/dlinear/`：DLinear逐点预测、全部指标、图、测试回执和checkpoint。
- `configs/`：冻结配置。
- `tests/`：因果性与产物测试。
- `vendor/tslib/`：固定提交的DLinear原始实现、许可证和来源哈希。
- 根目录 `*.py`：审计、baseline、DLinear、报告和独立验证代码。

## 复核与复现

只核对交付结果，不重新训练：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements-baselines.txt
.\.venv\Scripts\python.exe verify_dlinear_outputs.py
```

完整重跑DLinear需要NVIDIA CUDA环境：

```powershell
.\.venv\Scripts\python.exe -m pip install torch==2.7.1 --index-url https://download.pytorch.org/whl/cu128
.\.venv\Scripts\python.exe -m pip install -r requirements-dlinear.txt
$env:PYTHONUTF8='1'
.\.venv\Scripts\python.exe run_dlinear.py
```

程序只在配置、代码、训练数据和checkpoint哈希一致时复用checkpoint。当前训练环境为 Python 3.12、PyTorch 2.7.1+cu128、RTX 4060 Laptop GPU，细节见 `artifacts/dlinear/environment.json`。

## 本交接包没有包含

- `.venv`、pip缓存、PyTorch安装包和临时目录，总计约11 GB，可按requirements重建。
- 附件3、附件4与result模板，因为本阶段没有使用或写入它们。
- PatchTST、TimesNet、iTransformer、LP/MPC，因为当前工作明确停止在DLinear阶段。

`文件清单_SHA256.csv` 是本交接目录的逐文件大小和SHA256清单。原工作目录仍保留在 `D:/数学建模/CUMCM2026C_TS_Baseline`，本目录是独立副本。
